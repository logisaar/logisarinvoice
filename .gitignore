# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment files (never commit secrets)
.env
.env.*
!.env.example
**/.env
**/.env.*
*.pem
*.key
*.sqlite
*.db

# Deployment plan (contains server paths and ops notes — keep private)
DEPLOYMENT.md

# Production docker-compose override (internal port config)
docker-compose.prod.yml

# Uploaded files (logos etc. — stored on server, not in repo)
apps/api/uploads/
